{
  "name": "shp2stl",
  "private": true,
  "type": "module",
  "scripts": {
    "prepare:baseline": "node diagnostics/prepare-baseline.mjs",
    "test": "node --import ./diagnostics/register.mjs diagnostics/check.mjs",
    "benchmark": "node diagnostics/run.mjs"
  },
  "devDependencies": {
    "playwright-core": "1.55.0"
  }
}
